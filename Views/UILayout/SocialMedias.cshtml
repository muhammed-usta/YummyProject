@model List<YummyProject.Models.SocialMedia>


<div class="col-lg-3 col-md-6">
    <h4>Bizi Takip Edin</h4>
    <div class="social-links d-flex">

        @foreach (var item in Model)
        {
             
        <a href="@item.Url"  class="@item.Icon"> </a>
        }
   
    </div>
</div>

